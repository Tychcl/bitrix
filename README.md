# Установка и тд
1. скачать https://www.1c-bitrix.ru/download/vmbitrix.php HyperV windows 10
2. в hyperV `Импорт виртуальной машины`
3. когда будет пункт выбора копировать или восстановить, выбираем копировать
4. подключаем switch
5. запускаем виртуалку, заходим под root, пароль пишется у красных рамок рядом
6. меняем пароль, предложит автоматически
7. все, https://dev.1c-bitrix.ru/learning/course/index.php?COURSE_ID=32&LESSON_ID=29234&LESSON_PATH=3903.4862.29228.29234
   1. dnf clean all && dnf update
   2. wget http://repo.bitrix24.tech/dnf/bitrix-env-9.sh && chmod +x bitrix-env-9.sh && ./bitrix-env-9.sh
   3. Далее необходимо согласиться на отключение SELinux (если SELinux включен в системе) и перезагрузить машину командой reboot
   4. После перезагрузки сервера снова продолжите установку BitrixEnv: ./bitrix-env-9.sh
   5. Когда установка закончится , запустите меню BitrixEnv: /root/menu.sh
   6. В VMBitrix версии 9x+ нужно обязательно создать пул (1. Create Management pool of server). Мастер создания пула откроет все необходимые порты в CentOS для корректной работы сервисов продуктов «1С-Битрикс»
   7. cd ../
   8. cd home/bitrix/www
   9. wget https://www.1c-bitrix.ru/download/scripts/bitrixsetup.php
   


# Приколы с кастомными модулями
1. Модуля нет в `Настройки -> настройки продукта -> модули`
   Если модуль в папке `local/modules` стоить проверить `Marketplace -> Установленные решения`   
2. Установка
   Чтобы установить нужно так же перейти в `Marketplace -> Установленные решения` и слева от модуля нажать на 3 полосочки, там должен быть пункт меню - `установить`

# 7 задание
комплексное свойство, указываешь и чтобы проверить идешь в элемент инфоблока с комплексным свойством

# ./bitrix/.settings.php
через консоль `vi .settings.php`, редактировать `:i`, сохранить и выйти `:wq`
